export function GuestDetailsDrawer(_props: {
  guestId: string;
  activeTab: "profile" | "activity";
  onTabChange: (tab: "profile" | "activity") => void;
  onClose: () => void;
}) {
  return <div>drawer stub</div>;
}
