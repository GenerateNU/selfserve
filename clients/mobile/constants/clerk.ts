export enum ClerkStatus {
  Complete = "complete",
  MissingRequirements = "missing_requirements",
}

export enum ClerkErrorCode {
  SessionExists = "session_exists",
  IdentifierAlreadySignedIn = "identifier_already_signed_in",
}
